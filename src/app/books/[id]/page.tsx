import Footer from "@/app/(components)/Footer";
import Header from "@/app/(components)/Header";

export default function BookPage() {
  return (
    // Returns information on the book such as author, title, openlibrary link to it, etc"
    <div>
      <Header />
      <h1>Book</h1>
      <Footer />
    </div>
  );
}
